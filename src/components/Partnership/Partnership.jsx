import { HiArrowUpRight } from "react-icons/hi2";
import { partnerships } from "../../data/siteData";
import SectionHeading from "../ui/SectionHeading";

const Partnership = () => <section id="partnership" className="bg-white py-20 sm:py-28"><div className="mx-auto max-w-7xl px-5 sm:px-8"><SectionHeading centered eyebrow="PARTNERSHIP MODEL" title="Build the next padel destination with us." description="Pilih model kolaborasi yang paling sesuai dengan ambisi venue Anda." /><div className="mx-auto mt-12 grid max-w-5xl gap-5 lg:grid-cols-2">{partnerships.map((item) => <article key={item.title} className={`rounded-3xl p-7 sm:p-9 ${item.featured ? "bg-navy text-white shadow-2xl shadow-blue-950/20" : "border border-slate-200 bg-slate-50"}`}><p className={`text-xs font-extrabold tracking-[.18em] ${item.featured ? "text-blue-300" : "text-labsi-blue"}`}>{item.subtitle}</p><h3 className="mt-3 font-display text-3xl font-bold">{item.title}</h3><p className={`mt-4 min-h-14 text-sm leading-7 ${item.featured ? "text-slate-300" : "text-slate-600"}`}>{item.description}</p><div className={`my-8 grid grid-cols-2 rounded-2xl border p-5 ${item.featured ? "border-white/15 bg-white/5" : "border-slate-200 bg-white"}`}>{item.split.map((split) => <p key={split} className="text-center text-sm font-bold">{split}</p>)}</div><a href="#contact" className={`inline-flex items-center gap-2 text-sm font-bold ${item.featured ? "text-white" : "text-navy"}`}>Request Proposal <HiArrowUpRight className="text-lg" /></a></article>)}</div></div></section>;
export default Partnership;
